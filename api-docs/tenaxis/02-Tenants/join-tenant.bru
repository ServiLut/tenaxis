meta {
  name: Join Tenant
  type: http
  seq: 3
}

post {
  url: {{local}}/tenants/join
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "slug": "nuevo-tenant"
  }
}
