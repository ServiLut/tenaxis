meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{local}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "xl9luis@gmail.com",
    "password": "123456"
  }
}

script:post-response {
  if (res.body.access_token) {
    bru.setVar("token", res.body.access_token);
  }
}
